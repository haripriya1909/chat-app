// next.config.js
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["dummyjson.com", "robohash.org", "www.freepnglogos.com"], // Add both domains here
  },
};

export default nextConfig;
